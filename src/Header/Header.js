import React from 'react'
import '../main.scss'

export default function Header() {
  return (
    <header className="header">
      <h1>North Carolina Wines</h1>
      <button>Try Endpoints</button>
      <button>Available Endpoints</button>
    </header>
  )
}